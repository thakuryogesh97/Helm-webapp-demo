apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-webapp-demo.fullname" . }}
  labels:
    {{- include "helm-webapp-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-webapp-demo.selectorLabels" . | nindent 4 }}
